# ⚠️ 复制此文件为 .env.production 并填入真实值
# cp .env.production.example .env.production

NODE_ENV=production
DB_PASSWORD=<生成强密码: openssl rand -base64 32>
JWT_SECRET=<生成随机密钥: openssl rand -hex 32>
REDIS_PASSWORD=<生成强密码: openssl rand -base64 32>
CORS_ORIGIN=<你的域名,逗号分隔>
WEB3_PROVIDER_URL=<你的 RPC 节点 URL>
